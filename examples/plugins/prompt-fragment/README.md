# Chinese Prompt Fragment Plugin

A Metis plugin that injects a prompt fragment requiring the agent to always respond in Chinese.

## Behavior

After loading this plugin, every agent response will be in Chinese unless the user explicitly asks for another language.

## Usage

Install the plugin into your Metis plugins directory. No additional configuration is required.
