# Small Model Mode

Small mode limits tool schemas, persists oversized tool output, compresses context, requires strict JSON final output, repairs parser failures, and blocks repeated failed tool calls.

Finalization is also guarded: completion claims such as generated, tested, uploaded, or fixed must have typed supporting evidence from artifacts, evidence records, or specific tool results.
